export enum PermissionLevel {
  PUBLIC = 'PUBLIC',      // Read-only access to public endpoints
  PRIVATE = 'PRIVATE',    // Read-only access to most endpoints  
  ADMIN = 'ADMIN',        // Full system access
  // Add more as needed
}